version: 0.2

phases:
  build:
    commands:
      - echo Build Started on `date`
      - chmod +x ./gradlew
      - ./gradlew build
  post_build:
    commands:
      - echo Build completed on `date`
      - echo $(basename ./build/libs/*.jar)
      - pwd

artifacts:
  files:
    - appspec.yml
    - build/libs/*.jar
    - scripts/dev/**
  discard-paths: yes

cache:
  paths:
    - '/root/.gradle/caches/**/*'